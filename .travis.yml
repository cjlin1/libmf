dist: trusty
language: cpp

install:
    - make -e

matrix:
    - env: DFLAG="-DUSESSE -DUSEOMP"
    - env: DFLAG="-DUSEAVX -DUSEOMP"
    - env: DFLAG="-DUSEOMP"
    - env: DFLAG=""

script:
    - cd demo && sh demo.sh
